ok the last few tiny improvements/fixes needed are:
- please make sure the snake-braille spinner/loader on bottom right of the web-interface at the squid image works, it doesnt seem to be moving now. I also see a second snake-braille spinner that IS working on top center of the screen, maybe remove the bottom right one and move the currently top-center located one to the place we wanted on bottom right at the squid? so just above the squid on the left side, still inside the TV-like image.
- remove the bottom center text/placeholder "vQQVERSION" I see this in white a bit vague on bottom center of the screen, remove it.
- can you make sure on the agents page the 4 terminals are one-size, just covering the whole screen split into 4, so we can see the output of all 4 at one glance, and we can click them to get a single agent overview tailing the output that already works nicely.
- make sure when we go to the Config page, the config text is wrapped by our display size, not scrollbarred horizontally like it is now. so we only have a scrollbar for vertical scrolling and can see all the text without having to scroll sideways.
- please make the Act:Preparing indicator like the same color as the Total and Agent times, so the "Act:" in grey, and the actual action that is displayed, for example "PREPARING" is our typical yellowish color like the numbers of the times are from that Total: <time> and Agent: <time> indicators.
