Create a Python web scraper that fetches news headlines from a mock website, stores them in a SQLite database, and provides a simple command-line interface to search the database. It should handle errors, respect robots.txt (mock logic), and have a modular structure with separate files for database, scraper, and CLI. Ensure proper error handling and logging are implemented throughout the application.